Changes for short-term major profit gains (high-risk scalping):

1. AI/Prompt (aggressive):
    Prompt: "Aggressive scalper: target 0.3-0.8% gains/cycle, 1:3 RR, enter momentum breaks (RSI>70 buy oversold bounce, MACD cross), exit fast. Ignore ranging."
    Require "confidence >0.85", "size_percent_of_equity": 0.3-0.5.

2. Risk Params (.env):
    MAX_POSITION_SIZE_PCT=50 (50% equity/trade).
    MIN_CONFIDENCE=0.85.
    STOP_LOSS_PERCENT=0.2 (tight).
    TAKE_PROFIT_PERCENT=0.6 (3:1).
    MAX_HOLD_HOURS=0.1 (6min).
    DAILY_LOSS_LIMIT_PCT=10 (allow volatility).

3. Leverage/Entry:
    LEVERAGE=20.
    Entry only high vol (ATR>avg), volume spike.

4. Tech/Logic:
    Multi-TF: prompt 1m+5m alignment.

5. Model/Opt:
    LLM_PROVIDER=groq (fast llama3).
    CYCLE_MINUTES=0.5 (30s).

6. Test:
    FORWARD_TESTING=true, simulate 1 week.

Warning: High leverage/position = high risk ruin. Backtest first. No "sure" profits—markets random.

￼
￼
__Logic issues with stop_loss_price/take_profit_price:__

1. __AI suggestions ignored__: Prompt requests "stop_loss_price"/"take_profit_price" from AI, but trader.py `_parse_ai_outlook`/`_execute_trade` uses config % only (`_calculate_stop_loss`/`_calculate_take_profit`). AI values unused.

2. __No SL/TP orders placed__: monitor_positions closes market on price hit, but no standing SL/TP orders (risk slippage/gap).

3. __No override__: Config fixed % (0.5% SL), AI dynamic ignored (better for vol).

__Consequences:__

- Suboptimal levels (config generic).
- Manual close risk (downtime/miss).

__Recommend:__

- Parse AI SL/TP in `_parse_ai_outlook`.
- Use if provided, else config.
- In place_order or separate, place SL/TP orders (bitunix supports STOP_MARKET/TAKE_PROFIT_MARKET).
