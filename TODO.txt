Trading Bot Logic Issues:





4. Hardcoded risk params: risk_manager.calculate_position_size uses self.risk_params.max_position_size_pct=0.1 (hardcoded), ignores config.

5. Proposed size in can_trade: proposed_size = decision.get('quantity', ...) — 'quantity' not in AI decision.

6. monitor_positions unused: Defined in trader.py but not called in loop (missed exits).

7. Trader confidence hardcoded: _parse_ai_outlook overrides AI outlook.confidence with action-based values. Prefer AI confidence.

8. CRYPTO empty: '' if not in .env, RUN_NAME odd (e.g., LIVE_BITUNIX__BTCUSDT...).

9. Generic block reason: "Trade blocked by risk management" — log specific check (confidence/position/risk).
